import type { RuviaConfig } from "./types.d.ts";

export const RuviaConfig: RuviaConfig = {
    slashCommands: {
        options: {
            noPermissionMessage: "You can't use this command."
        }
    }
}